# keengen Entware — scaffold config
# Bind: LAN interface address or 0.0.0.0 for all LAN IPs (never expose to WAN).
KEENGEN_BIND=0.0.0.0
KEENGEN_PORT=1001
KEENGEN_WWW=/opt/share/keengen/www
# When API binary appears:
# KEENGEN_API=0
